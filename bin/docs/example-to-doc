#!/bin/sh

export EXAMPLE=$1
export EXNAME=`echo ${EXAMPLE} |cut -c 3-`
cd examples; \
install -d dirname ${EXAMPLE} 
echo "# ${EXNAME}\n\`\`\` c++\n" > ${EXAMPLE}.md
cat ../../examples/${EXAMPLE} >> ${EXAMPLE}.md
echo "\n\`\`\`"  >> ${EXAMPLE}.md


